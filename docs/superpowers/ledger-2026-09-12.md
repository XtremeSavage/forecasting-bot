# SDD ledger — plan: docs/superpowers/plans/2026-09-11-forecasting-bot.md
Spec: docs/superpowers/specs/2026-09-11-forecasting-bot-design.md (read; binding authority)
Branch: build/v1 (created from master @7e94ad0)

## Preflight scan
| Pair / task | Produces vs consumes | Finding |
|---|---|---|
| T3 models <-> T5,T6,T7,T9,T10,T13 | ForecastValue/QuestionSummary/MemberForecast/Aggregate/ForecastRecord fields | consistent (percentiles keyed int; kind literal) |
| T4 Llm <-> T7,T10 fakes | complete(prompt, model, temperature, system, max_tokens); complete_json(prompt, model, out_type, temperature, retries, system, max_tokens) | consistent, positional order matches |
| T5 aggregate <-> T10 pipeline | aggregate(), aggregate_binary(), aggregate_mc(), bounded_logit_shift(), percentiles_to_cdf() | consistent |
| T6 guards <-> T10 | drop_invalid_members, enough_members, validate_value, Budget | consistent |
| T7 forecast.run_member <-> T10 | parse failure returns MemberForecast(dropped_reason=...) ; pipeline filters dropped before validation | consistent |
| T8 research.run <-> T10 | run(llm, settings, forensics, criteria_text, background_text) -> (bundle, cost) | consistent |
| T9 records <-> T10,T11,T13 | write(rec, runs_dir); already_forecasted_locally(post_id, runs_dir); question_summary(q) | consistent; question_summary relies on forecasting-tools get_question_type() semantics (verify at T9) |
| T9 self | test _rec() has published=False but already_forecasted_locally requires published=True | plan text itself flags this; Ruling below |
| T10 self | DA bound test: logit(~0.235)+0.5 -> ~0.34 < 0.45 | arithmetic holds |
| T10 MetaculusPublisher | uses QuestionState.OPEN from forecasting_tools.data_models.questions | verify enum name at T10 |
| T11 <-> T12 | run.py exit codes (0 clean, 1 guards, 2 season cap) vs workflow "Fail job if guards fired" | consistent |
| T1 pyproject | anthropic "^0.60.0" may not match current SDK | Ruling below |
| T12 workflow | `echo exit=$?` after failing command under set -e | plan note already prescribes `set +e`; implementer must apply |

## Rulings (preflight)
- Ruling: work on branch build/v1 in place, no separate worktree — venv and OneDrive path are local to this dir; solo repo — cost if wrong: none material.
- Ruling: T9 write test must construct its record with published=True before asserting already_forecasted_locally — the plan's stated semantics (published-only) win over the fixture literal — cost if wrong: none.
- Ruling: T1 pins anthropic to whatever the latest version pip resolves on 2026-09-12 (record the pin), not "^0.60.0" — cost if wrong: a dependency bump later.
- Ruling: git author set repo-locally to XtremeSavageXD; every implementer commit carries the session attribution trailer.

## Task log
- 2026-09-12 00:xx XtremeSavageXD offline ~9h. Authorized: finish all tasks, no paid runs (no dry run, no live triggers), create public GitHub repo + push, copy project to ~/projects/forecasting-bot, update all .md/design docs for handoff.
- Ruling: Task 11 Step 5 (paid dry run) and Task 2 Step 7 / Task 12 Step 7 live triggers are deferred to XtremeSavageXD; Task 13 Step 4 (needs .env) deferred — cost if wrong: none, they are gated on secrets anyway.
- Ruling: folder move = copy with git history to projects/, fresh venv there, leave MOVED.md in the OneDrive folder rather than deleting it mid-session — cost if wrong: XtremeSavageXD deletes one folder by hand.
Task 1: implementer DONE (commit 77c0261); review dispatched.
Task 1: fix round 1/5 (1 addressed pending re-review — pytest-asyncio pin; commits 77c0261..e16fa75)
Task 1: complete (commits 6ff583f..e16fa75, review clean after 1 fix round)
Task 2: implementer DONE (a9a5d02); review dispatched
Task 2: complete (commits e16fa75..a9a5d02, review clean)
Task 2: minor (deferred): workflows have no explicit permissions: block; action refs pinned by tag not SHA (plan-mandated)
Task 3: implementer DONE (4f736b3); review dispatched
Task 3: complete (commits a9a5d02..4f736b3, review clean)
Task 4: implementer DONE (1fc30f4); review dispatched. Note for live smoke: anthropic SDK 1.5.0 messages.create has no temperature kwarg, passed via extra_body; HttpTransport not covered by tests — first paid run must exercise both providers.
Task 4: review found 3 Important (plan-mandated). Ruling: fix all three — (a) add a test for the post-call ceiling branch, (b) wrap Anthropic-fallback failures in LlmError so callers see one exception surface, (c) make extract_json take the whole fenced block (nested objects are the norm for Forensics/EvidenceTable) with the find/rfind fallback kept — spec 3 says every stage returns structured JSON, so brittle parsing is load-bearing. Cost if wrong: small parser rework.
Task 4: minor (deferred): broad except in complete() hides transport bugs; fallback_used is sticky per Llm instance (pipeline creates one Llm per question, so acceptable).
Task 4: fix round 1/5 (3 fixed pending re-review; commits 1fc30f4..2866847)
Task 4: complete (commits 4f736b3..2866847, review clean after 1 fix round)
Task 5: implementer DONE (a8fa20d) with deviations: aggregate_mc closed-form floor (brief's version fails its own test), pyproject filterwarnings for asknews_sdk pydantic deprecation, get_cdf() over .cdf; review dispatched
Task 5: review Needs fixes — Important: aggregate_mc returns sum>1 when n*floor>=1. Fix round 1 dispatched.
Task 5: minor (deferred): no tests for open bounds / zero_point log axis (plan-mandated test set); np.maximum.accumulate is a defensive no-op; aggregate() has no empty-list or mixed-kind guard.
Task 5: fix round 1/5 (1 fixed pending re-review; commits a8fa20d..6805959)
Task 5: complete (commits 2866847..6805959, review clean after 1 fix round)
Task 6: implementer DONE (e0a10b8); review dispatched
Task 6: review Needs fixes — Important (plan-mandated): season_spent crashes on wrong-shape JSON. Ruling: fix by catching (JSONDecodeError, OSError, ValueError, TypeError, AttributeError) — spec 4 says the season cap must be robust. Fix round 1 dispatched.
Task 6: minor (deferred): consistency_ok direct-call None handling; p10/p90 fallback to min/max; Budget zero-division with zero config; edge-case tests absent (plan-mandated test set).
Task 6: ⚠️ resolved by controller: no double counting in season_spent — records.write (Task 9) writes exactly one JSON per question per run, scores.csv is not JSON, dashboard data.json lives under docs/. Dry-run records carry real spend and should count.
Task 6: fix round 1/5 (1 fixed pending re-review; commits e0a10b8..427e660)
Task 6: complete (commits 6805959..427e660, review clean after 1 fix round)
Task 7: implementer DONE (42c26f1); review dispatched
Task 7: parked — Important (plan-mandated): base_rate.run / evidence.run / run_member / devils_advocate.run have no executing test in test_stages.py — Ruling: Task 10's test_full_binary_run drives all five stages through a FakeLlm end to end, which is the intended coverage; final review must confirm it runs every stage. Cost if wrong: a stage regression caught one task later.
Task 7: minor (deferred): devils_advocate imports private _fmt_value from forecast; substring-based prompt tests.
Task 7: complete (commits 427e660..42c26f1, 1 parked)
Task 8: implementer DONE (b744642), deviation: trafilatura favor_precision=True; review dispatched
Task 8: complete (commits 42c26f1..b744642, review clean)
Task 8: minor (deferred): extract_urls(criteria_text) computed twice in run(); except Exception won't catch CancelledError; favor_precision=True may drop body text on some pages.
Task 9: implementer DONE (acfd7e7); get_question_type() returns the api string directly; review dispatched
Task 9: review Needs fixes — Important (plan-mandated): comment truncation can drop Final line and footer. Ruling: fix by reserving the tail (Final + footer) and truncating only the head sections — spec 3 requires the comment to carry the final number. Fix round 1 dispatched.
Task 9: minor (deferred): records.write assumes tz-aware run_ts (pipeline always emits UTC-aware); Date/Discrete question_summary branches untested.
Task 9: fix round 1/5 (1 fixed pending re-review; commits acfd7e7..3e36436)
Task 9: complete (commits b744642..3e36436, review clean after 1 fix round)
Task 10: implementer DONE (ec0ee02); review dispatched
Task 10: review Needs fixes — 4 Important (3 plan-mandated). Ruling: fix all — (1) gate stage 4 on stages.ach_forecast (off -> guard SKIP_ACH_OFF, no publish), (2) cost accounting reconciled against llm.total_cost_usd delta so failed calls still count and the DA brake uses real spend, (3) PROVIDER_FALLBACK only if fallback_used flipped during this question, (4) DA stage wrapped: failure -> guard DA_FAILED, keep pre-DA. Spec 4 (guards must be trustworthy) and spec 6 (cost control) make these load-bearing. Cost if wrong: small.
Task 10: minor (deferred): question_summary/Budget above the try can raise (Task 11 must guard per question); DA_SKIPPED_BUDGET/DA_REVISION_INVALID/PIPELINE_ERROR/PROVIDER_FALLBACK/MC+numeric DA branches/MetaculusPublisher untested; partial publish (comment failure) recorded as unpublished; is_open treats close_time None as open; Budget.exhausted unused; unused imports in test.
Task 7 parked finding resolved: test_full_binary_run_with_bounded_da drives all five stage runs (structurally, via the 9-text FakeLlm queue).
Task 10: fix round 1/5 (4 fixed pending re-review; commits ec0ee02..07e269a)
Task 10: complete (commits 3e36436..07e269a, review clean after 1 fix round)
Task 11: implementer DONE (a6e4de2), paid dry run deferred; review dispatched
Task 11: complete (commits 07e269a..a6e4de2, review clean). Paid dry run (Step 5) deferred to XtremeSavageXD.
Task 11: minor (deferred): --limit 0 treated as unset; guards accumulation outside the per-question try.
Task 12: implementer DONE (ba8206a), repo push deferred to controller; review dispatched
Task 12: complete (commits a6e4de2..ba8206a, review clean). Repo creation/push deferred to controller (Task 16).
Task 12: minor (deferred): commit step assumes runs/ tracked (it is, via .gitkeep); cup.yaml has no workflow_dispatch.
Task 13: implementer DONE (f58f6aa), CP key path discovery deferred; review dispatched
Task 13: review Needs fixes — Important: log of 0 when cp==1.0 on a No; missing total-cost print (plan-mandated). Ruling: clamp inputs to [0.001, 0.999] (Metaculus's own binary clamp) inside binary_log_score; add total cost line. Fix round 1 dispatched.
Task 13: minor (deferred): happy-path-only tests for load_records/join edge cases.
Task 13: fix round 1/5 (2 fixed pending re-review; commits f58f6aa..a035cda)
Task 13: complete (commits ba8206a..a035cda, review clean after 1 fix round). CP key path still UNVERIFIED (needs credentials).
Task 14: implementer DONE (bdab148); review dispatched
Task 14: review Approved with 1 Important (href scheme allowlist missing). Fix round 1 dispatched.
Task 14: minor (deferred): MC branch of _format_forecast untested; kind detection by key presence; scores post_id coerced to float (inert, nothing joins on it).
Task 14: fix round 1/5 (1 fixed pending re-review; commits bdab148..e24245d)
Task 14: complete (commits a035cda..e24245d, review clean after 1 fix round)
Final whole-branch review dispatched on 7e94ad0..e24245d
Final review (opus): 1 Critical (numeric hang: np.interp clamp + no per-question timeout), 10 Important, minors. Verdict: merge with fixes; dry run with fixes.
Rulings for the fix wave (see final-fix-brief.md): fix C1, I1, I2, I4, I5, I6, I8, I9, I10 and the small minors; I3 (date questions) ruled SKIP with guard SKIP_DATE_UNSUPPORTED since Fall uses no date questions — cost if wrong: date questions on the Cup get no forecast; I7 stays a dry-run checklist item (needs real API); I8 gate = repo variable BOT_LIVE so merging to main is not the go-live action. Later: guard-noise split, unclosed clients, repo growth, spec naming, re-forecast strategy.
Fix wave dispatched (one implementer, opus).
Final fix wave DONE (c22c998, e9ea642, b097e46, ecb3675; 89 tests). Scoped re-review dispatched on c398dea..HEAD.
Final fix wave re-review (opus): all findings ADDRESSED, no new Critical/Important breakage, 89 tests, ready for a supervised paid dry run.
Residuals parked with rulings:
- control_cup.yaml has no workflow_dispatch, so the BOT_LIVE gate makes it inert until the variable is set — Ruling: park; one-line follow-up before the dry run, listed in HANDOFF — cost if wrong: the control cup bot cannot be rehearsed by hand.
- Control workflows do not pass ANTHROPIC_API_KEY, so the control bot has no fallback route — Ruling: park; the control is meant to be the stock template, adding keys changes nothing in its code but is XtremeSavageXD's call — cost if wrong: control bot misses questions during an OpenRouter outage.
- When OpenRouter is down all four members become the same Anthropic fallback model — Ruling: accepted as designed; PROVIDER_FALLBACK records it — cost if wrong: less ensemble diversity during outages.
- Time half of Budget.exhausted unreachable under run.py's outer timeout; tie-bump can nudge a top percentile above a closed bound; research fan-out up to 15 concurrent search calls — Ruling: later; noted in HANDOFF.
- A timed-out question's record has published=False and no guard string — Ruling: acceptable per brief (log + count); later add a QUESTION_TIMEOUT guard.
SDD complete. Workspace copied to docs/superpowers/ledger-2026-09-12.md.
